# Lightweight requirements for the FastAPI dashboard API only.
# Used on Render — excludes ingestion/NLP/pipeline deps (playwright, spacy, etc.)

fastapi>=0.138.0
uvicorn[standard]>=0.49.0
sqlalchemy>=2.0.30
python-dotenv>=1.0.1
pyarrow>=16.0.0
pandas>=2.2.2
